# Production .env for dashboard/  (copy to dashboard/.env BEFORE `npm run build`)
# This value is baked into the static bundle at build time — rebuild if you change it.
VITE_LOCAL_API_BASE_URL=https://api.example.com/
