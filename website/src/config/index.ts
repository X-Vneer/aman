// !!add new lang to middleware mather
export const LOCALES = ["ar", "en", "fr", "id"] as const
export const RTL_LOCALES = ["ar"] as const
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL || "http://localhost:3000"
