# Root monorepo ignores. Each subproject also keeps its own .gitignore.
/node_modules
npm-debug.log*
yarn-error.log*

# OS / editor
.DS_Store
Thumbs.db

# Local env
.env
.env.local

# Local DB dumps — contain real user data, keep out of version control
aman-db.sql.zip
*.sql
*.sql.zip
